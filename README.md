# mydjango

Django project with Docker and PostgreSQL.

## Tech stack
- Python
- Django
- PostgreSQL
- Docker / Docker Compose

## Run project
```bash
git clone https://github.com/NikoBarishnikov/mydjango.git
cd mydjango
cp .env.example .env
docker-compose up --build
